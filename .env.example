# PostgreSQL Database Configuration
# These variables are used by docker-compose to provision the database.
POSTGRES_USER=codeweaver
POSTGRES_PASSWORD=
POSTGRES_DB=codeweaver

# Prisma Database URL
# This is used by the Prisma client to connect to the database.
# The host should be 'localhost' for local development since the port is forwarded.
DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<db>"

# AI Providers API Keys
# Add your OpenRouter API key here.
OPENROUTER_API_KEY= 